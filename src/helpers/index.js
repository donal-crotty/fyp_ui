import * as authHeader from '../helpers/auth-header';
import * as config from '../helpers/config';
import * as history from '../helpers/history';
import * as store from '../helpers/store';

export default { authHeader, config, history, store };
